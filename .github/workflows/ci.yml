name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  smoke:
    strategy:
      matrix:
        node-version: [20.x, 22.x]
    uses: ./.github/workflows/_gates.yml
    with:
      node-version: ${{ matrix.node-version }}
